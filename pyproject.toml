[project]
name = "throw-a-ball"
version = "0.12.0"
description = "Throw A Way Games presents Throw a Ball - Dartsnut skee-ball arcade prototype."
requires-python = ">=3.11"
dependencies = [
    "pydartsnut==1.2.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.uv]
package = false
